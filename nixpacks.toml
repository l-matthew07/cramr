providers = ["node"]

[phases.setup]
nixPkgs = ["nodejs_22", "openssl"]
nixLibs = ["openssl"]

[phases.install]
cmds = [
  "corepack enable",
  "corepack prepare pnpm@9.15.9 --activate",
  "pnpm install --frozen-lockfile"
]

[phases.build]
cmds = ["pnpm run build"]
